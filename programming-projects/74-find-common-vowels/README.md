# 74. Find Common Vowels

## 题目
给定两个小写字符串，返回二者都出现过的元音，去重并按字母顺序连接；没有则为空串。接口 `solution(string1,string2)`。

样例：`float/arose` 输出 `ao`；`equivalent/fraction` 输出 `ai`。

## 解析
将两个字符串转集合，再按固定的字母序 `aeiou` 检查元音是否同时存在，满足则加入结果。

## 正确性与复杂度
集合准确记录字符是否出现，固定顺序保证唯一且有序，故结果恰是共同元音集合。时间 `O(n+m)`，空间 `O(n+m)`。测试覆盖原题两例。
