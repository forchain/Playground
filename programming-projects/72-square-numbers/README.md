# 72. Square Numbers

## 题目
判断整数平方的末尾数字是否与该整数相同，是则返回 1，否则返回 0，范围 `[-10000,10000]`。接口 `solution(n)`。

样例：25 的平方 625 以 25 结尾，输出 1；8 的平方 64 不以 8 结尾，输出 0。

## 解析
非负数有 `d` 位时，末 `d` 位等于 `n² mod 10^d`。负数不可能是非负平方的十进制后缀，直接返回 0。

## 正确性与复杂度
十进制取模恰好保留指定数量的末位，因此等式与题意等价。时间和额外空间均为 `O(1)`（在给定整数范围下）。测试覆盖两例。
